# 单元测试目录

此目录用于存放单元测试文件。

测试文件命名规范：
- 与源文件同名，添加 `.test.ts` 或 `.spec.ts` 后缀
- 示例：`utils.test.ts` 或 `utils.spec.ts`

示例结构：
```
tests/unit/
├── utils.test.ts
├── hooks/
│   └── useLocale.test.ts
└── services/
    └── api.test.ts
```
